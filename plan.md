1. Add `formatFullDate` in `apps/waterfall-flow-control/waterfall/utils/waterfallData.ts`.
2. Update `CustomTooltip` in `apps/waterfall-flow-control/waterfall/WaterfallChart.tsx` to import and use `formatFullDate`.
3. Add a journal entry in `.jules/bolt.md` documenting this finding.
4. Run tests and lint.
5. Submit PR.
