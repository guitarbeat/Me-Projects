// This hook is deprecated and replaced by React-Konva logic.
// Keeping empty file structure to prevent import errors in untracked files if any.
export const useCanvasImage = (imageUrl: string) => {
  return { isReady: false };
};